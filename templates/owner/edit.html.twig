{% extends 'base.html.twig' %}

{% block title %}Modification propriétaire{% endblock %}

{% block body %}
    <h1>Modifier le profil propriétaire</h1>

    {{ include('owner/_form.html.twig', {'button_label': 'Modifier'}) }}

    {# <a href="{{ path('owner_index') }}">back to list</a> #}
    </br>
    {{ include('owner/_delete_form.html.twig') }}
{% endblock %}
